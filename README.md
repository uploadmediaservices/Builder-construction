# Builder Construction

A small Python utility for generating rough construction estimates.

## Features
- Calculate material quantities with a waste factor.
- Calculate labor and material costs.
- Produce a readable estimate summary.

## Quick start
```bash
python -m src.builder
```

## Run tests
```bash
python -m unittest discover -s tests -p 'test_*.py'
```
